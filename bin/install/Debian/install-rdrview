#!/bin/sh
## install dependencies
sudo apt install libxml2-dev libseccomp-dev libcurl4-gnutls-dev
## clone repo
cd "$HOME/Repos/github.com"
git clone https://github.com/eafer/rdrview
cd rdrview
make
mv rdrview "$HOME/.local/bin/"
